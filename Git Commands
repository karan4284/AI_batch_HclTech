git --version 

git init

git config --global user.name "<yourName>"

git config --global user.email "<your mail ID>"

git config --global core.editor "code --wait"

git config --list


In VS Code : 
Create a file : abc.txt:
Hello world

save the file

The file becomes U : Unstashed/Uncommitted

Now go to terminal:
git add abc.txt

The file became : A (added in branch)



Now go to terminal:
 git commit -m "First commit : abc.txt"

The file gets commited


Now edit the file:
abc.txt:
Hello World
Modified Abc


save the file


The file became : M (Modified in branch, which was already comitted)

Now again add and commit with message "Second commit with modified abc.txt"

In Github:
Hello.html:
<html>
  <body>
    <h1>
      H-Hyper : Being Super Interactive (HTML is very interactive as 
      it can be executed in any browser, and can get the data from 
      anywhere in the Web
      <ul> Some features of HTML which make it hyper:
        <li>Anchor tag "a"</li>
        <li>Forms</li>
      </ul>
      <br>     
      T-Text: 
      in HTML Text means any visible component in a web page. These reside in the body of the page.
      Text can be plain text, images, video, links, tables, etc
      <br>
      M - Markup : In HTML the tags and styling are major part that describe the metadata of the page.
      Markup in English means formating. We do formatting in a web page with the help of tags and CSS
      <br>
      L-Language : All Computer Languages have common features of having:
      <ul>
        <li>Variables: placeholders to actual value</li>
        <li>Statements: lines of codes, like sentences</li>
        <li>Control structures: if..else if.. else, switch case</li>
        <li>Loops: repeatative structures</li>
        <li>Functions: group of statements solving a particular problem</li>
      </ul>
      In HTML we have Javascript handling the language part.   
      <br>
    </h1>
    <h2>
      CSS: 
      Cascading: following the HTML file
      Style: style the HTML content
      Sheet: the place where we write the style code
    </h2>
    <h2> 
      JS : JavaScript is the language behind HTML
    </h2>
    <h2>NodeJs : NodeJs is a Front End Server to render JavaScript.</h2>
  </body>
</html>








git clone clones the repository into our local current folder 
as a new folder with the same name as the repository name, with git initialized:

git clone https://github.com/monicag-gupta/Proj1.git


If fatal SSH (https security) error occurs:

git -c http.sslVerify=false clone https://github.com/monicag-gupta/Proj1.git

cd Proj1
Edit hello.html


<html>
  <body>
    <h1>
      H-Hyper : Being Super Interactive (HTML is very interactive as 
      it can be executed in any browser, and can get the data from 
      anywhere in the Web
      <ul> Some features of HTML which make it hyper:
        <li>Anchor tag "a"</li>
        <li>Forms</li>
      </ul>
      <br>     
      T-Text: 
      in HTML Text means any visible component in a web page. These reside in the body of the page.
      Text can be plain text, images, video, links, tables, etc
      <br>
      M - Markup : In HTML the tags and styling are major part that describe the metadata of the page.
      Markup in English means formating. We do formatting in a web page with the help of tags and CSS
      <br>
      L-Language : All Computer Languages have common features of having:
      <ul>
        <li>Variables: placeholders to actual value</li>
        <li>Statements: lines of codes, like sentences</li>
        <li>Control structures: if..else if.. else, switch case</li>
        <li>Loops: repeatative structures</li>
        <li>Functions: group of statements solving a particular problem</li>
      </ul>
      In HTML we have Javascript handling the language part.   
      <br>
    </h1>
    <h2>
      CSS: 
      Cascading: following the HTML file
      Style: style the HTML content
      Sheet: the place where we write the style code
    </h2>
    <h2> 
      JS : JavaScript is the language behind HTML
    </h2>
    <h2>
      TS: TypeScript is the new language which is strongly typed
      TS gets compiled into JS and then executed in HTML.
    </h2>
    <h2>NodeJs : NodeJs is a Front End Server to render JavaScript.</h2>
  </body>
</html>






git add .
git commit -m "Hello.html changed"





Know about Remote:
git remote –v


Know about branches:
git branch



push a branch to remote:
git push -u origin main

If fatal SSH error occurs:

git config --global http.sslBackend schannel
and then:
git push -u origin main

You need to authenticate the github account
and then check the repository for the updated content


Two ways to create a new branch:
1) Create and Switch to a New Branch
    git checkout -b feature-branch
2) Create Multiple Branches, but remain at the main branch
    git branch feature-login
    git branch feature-payment
Later go to the branch by 
    git checkout feature-branch

Delete a branch:
Delete Branches usually done after Merge
git branch -d feature-login



Step : Create the branch dev :
git checkout -b feature-dev

Add Hello.js

function clickme(){
    alert("Button clicked");
}


Edit Hello.html:

<html>
  <head>
    <script src="Hello.js"></script>
  </head>
  <body>
    <button onclick="clickme()">Click Here</button>
    <h1>
      H-Hyper : Being Super Interactive (HTML is very interactive as 
      it can be executed in any browser, and can get the data from 
      anywhere in the Web
      <ul> Some features of HTML which make it hyper:
        <li>Anchor tag "a"</li>
        <li>Forms</li>
      </ul>
      <br>
      T-Text: 
      in HTML Text means any visible component in a web page. These reside in the body of the page.
      Text can be plain text, images, video, links, tables, etc
      <br>
      M - Markup : In HTML the tags and styling are major part that describe the metadata of the page.
      Markup in English means formating. We do formatting in a web page with the help of tags and CSS
      <br>
      L-Language : All Computer Languages have common features of having:
      <ul>
        <li>Variables: placeholders to actual value</li>
        <li>Statements: lines of codes, like sentences</li>
        <li>Control structures: if..else if.. else, switch case</li>
        <li>Loops: repeatative structures</li>
        <li>Functions: group of statements solving a particular problem</li>
      </ul>
      In HTML we have Javascript handling the language part.   
      <br>
    </h1>
    <h2>
      CSS: 
      Cascading: following the HTML file
      Style: style the HTML content
      Sheet: the place where we write the style code
    </h2>
    <h2> 
      JS : JavaScript is the language behind HTML
    </h2>
    <h2>
      TS: TypeScript is the new language which is strongly typed
      TS gets compiled into JS and then executed in HTML.
    </h2>
    <h2>NodeJs : NodeJs is a Front End Server to render JavaScript.</h2>
  </body>
</html>






To see the branches:
 git branch




save the files

go to terminal:
git add .

git commit -m "Hello.js added"

Go back to main:
git checkout main


Step : Create the branch dev :
git checkout -b feature-design

Add Hello.css

h2{
    color:blue;
}
h1{
    color:green;
}


Edit Hello.html

<html>
  <head>
    <link rel="stylesheet" href="Hello.css"/>    
  </head>
  <body>
    <h1>
      H-Hyper : Being Super Interactive (HTML is very interactive as 
      it can be executed in any browser, and can get the data from 
      anywhere in the Web
      <ul> Some features of HTML which make it hyper:
        <li>Anchor tag "a"</li>
        <li>Forms</li>
      </ul>
      <br>      
      T-Text: 
      in HTML Text means any visible component in a web page. These reside in the body of the page.
      Text can be plain text, images, video, links, tables, etc
      <br>
      M - Markup : In HTML the tags and styling are major part that describe the metadata of the page.
      Markup in English means formating. We do formatting in a web page with the help of tags and CSS
      <br>
      L-Language : All Computer Languages have common features of having:
      <ul>
        <li>Variables: placeholders to actual value</li>
        <li>Statements: lines of codes, like sentences</li>
        <li>Control structures: if..else if.. else, switch case</li>
        <li>Loops: repeatative structures</li>
        <li>Functions: group of statements solving a particular problem</li>
      </ul>
      In HTML we have Javascript handling the language part.   
      <br>
    </h1>
    <h2>
      CSS: 
      Cascading: following the HTML file
      Style: style the HTML content
      Sheet: the place where we write the style code
    </h2>
    <h2> 
      JS : JavaScript is the language behind HTML
    </h2>
    <h2>
      TS: TypeScript is the new language which is strongly typed
      TS gets compiled into JS and then executed in HTML.
    </h2>
    <h2>NodeJs : NodeJs is a Front End Server to render JavaScript.</h2>
  </body>
</html>


save the files

go to terminal:
git add .

git commit -m "Hello.css added hello.html edited"

Go back to main:
git checkout main





If Github is not showing correct data:
Forcefully push all main branch data into github repo:
git push --force origin main


Push All Branches to Remote:
git push origin feature-dev
git push origin feature-design


List the Commit History:
To find the commit hashes:
git log --oneline --graph --all


Move to main branch
git checkout main

Merge the branches: In Parent branch we can merge child branches
git merge feature-design
If conflict occurs: 
First resolve it in VS code
Then Merge again:
git merge feature-design

git merge feature-dev



For GitLab:

Prepare a new project in gitlab with Readme file
Make it public.


Then in local system:

git config --global http.sslBackend openssl
git clone https://gitlab.com/gupta.monicag/leavesmanagementsys.git


cd leavesmanagementsys

create a new file :
Leaves.txt:
This is a new file made in local system.

Save
Add in git
commit as "New file added"

git push origin main

Check in the project of gitlab if the new file is added.
Edit the file in gitlab:
Leaves.txt:
This is a new file made in local system.
Now Edited in Gitlab

Commit

Now go to local system:
git pull

Check to find the contents are updated.







